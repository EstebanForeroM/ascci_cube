use super::Pixel;

pub fn line_renderer(matrix: &mut Vec<Vec<Pixel>>, points: [[f64; 3]; 2]) {
    let point1 = points[0];
    let point2 = points[0];
}

#[cfg(test)]
mod test {
    use super::*;

    fn line_render_test() -> {
        
    }
}
